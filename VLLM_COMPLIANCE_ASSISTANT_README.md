# VLLM 合规助手说明

> 状态：特定运行场景说明，不作为当前项目统一主入口。

## 文档定位

这份文件对应的是一条“本地 VLLM 模型接入 + 特定启动方式”的运行场景说明。它不是整个 Drass 项目的总 README，也不适合继续描述成“当前版本已经完整恢复的统一产品说明”。

## 为什么需要校正

旧版内容的问题主要有：

1. 把特定 VLLM 场景写成了当前项目统一主线。
2. 把 `8888`、`8001` 等端口关系写成默认事实，但这些端口依赖具体启动路径。
3. 把某些 UI / Sessions / 功能状态写成统一结论，缺少对场景边界的说明。

## 当前更准确的理解方式

这份文件现在只应表达：

1. 仓库支持接入本地 OpenAI-compatible / VLLM 风格模型服务。
2. 某些启动路径会采用 `LLM_BASE_URL=http://localhost:8001/v1` 这样的方式接入模型。
3. 是否使用 `8888`、是否使用特定启动脚本，应以当前部署路径为准。

## 当前建议参考的主文档

1. `README.md`
2. `docs/ONE_CLICK_STARTUP_GUIDE.md`
3. `docs/LLM_API_CONFIG_GUIDE.md`
4. `docs/chensha_运行依赖分析.md`
5. `docs/chensha_部署与基础设施规则.md`

## 本文件保留用途

保留该文件，仅作为本地 VLLM / OpenAI-compatible 模型接入场景的历史说明或场景补充，不再承担项目主入口职责。
