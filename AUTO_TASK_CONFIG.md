# 自动任务协作说明

> 状态：本文件是仓库内的协作建议，不是系统级强制行为配置，也不能覆盖实际的权限、审批、分支或工具限制。

## 文档目的

这份文件用于说明：当仓库中存在任务清单类文档时，应如何更安全、可控地配合任务推进，而不是把代理工具变成“默认无条件自动执行器”。

## 适用范围

适用于以下场景：

1. 仓库中存在 `TASK_LIST*.md`、任务清单、阶段任务单、实施任务表等文档。
2. 用户明确要求“继续”“执行下一个”“按任务列表推进”。
3. 需要根据任务单梳理优先级、依赖关系和当前进度。

不适用于以下场景：

1. 用户没有明确授权继续执行。
2. 任务清单明显过时、与当前代码不一致。
3. 需要删除、回滚、部署、推送、提交等高风险操作但用户未明确要求。

## 基本原则

### 1. 用户指令优先

只有在用户明确要求继续、执行或按任务列表推进时，才应进入任务驱动模式。

### 2. 文档不等于事实

任务清单只能作为参考输入，不能直接等同于当前项目真实状态。实际执行前应优先核对：

1. `README.md`
2. `docs/ONE_CLICK_STARTUP_GUIDE.md`
3. 当前代码
4. 当前启动脚本和配置

### 3. 不默认自动批准

以下行为不能因为存在任务清单就默认视为已批准：

1. `git commit`
2. `git push`
3. 部署或发布
4. 安装系统级依赖
5. 删除文件
6. 修改 CI/CD、生产配置、密钥、权限相关内容

### 4. 不默认连续执行

完成一个任务后，不应自动假设用户希望继续执行下一个任务。应根据用户当前目标和现场情况判断是否继续。

## 推荐触发方式

建议只在以下明确输入下进入任务推进模式：

- `继续`
- `继续执行任务`
- `按任务列表继续`
- `执行下一个任务`
- `梳理任务并开始处理`

不建议把“无明确指令时自动执行任务”作为默认规则。

## 推荐执行流程

当用户明确要求按任务列表推进时，建议使用以下流程：

1. 找到相关任务清单文档
2. 判断该任务清单是否仍与当前仓库一致
3. 识别当前最合理的下一项任务
4. 先做必要的代码/文档/配置核对
5. 实施修改
6. 做最基本的验证
7. 向用户汇报本次完成内容、验证情况和剩余问题

## 推荐任务状态语义

如果需要维护任务单，建议使用更保守的状态定义：

| 状态 | 含义 |
| --- | --- |
| `PENDING` | 尚未开始 |
| `IN_PROGRESS` | 正在处理 |
| `BLOCKED` | 被依赖、权限、环境或事实冲突阻塞 |
| `COMPLETED` | 已完成并有基本验证 |
| `CANCELLED` | 明确不再处理 |

## 建议的判断规则

### 何时可以继续做

满足以下条件时，可以继续处理任务：

1. 用户有明确继续指令
2. 当前任务与项目主线相关
3. 任务未明显过时
4. 所需改动不涉及未授权高风险操作

### 何时应停下来

出现以下情况时，应停止自动推进并向用户说明：

1. 任务单与当前仓库明显冲突
2. 需要部署、推送、提交或删除
3. 发现关键环境信息不明确
4. 发现任务本身已经失效
5. 现场存在与当前目标冲突的用户改动

## 不推荐的旧规则

以下做法不应继续作为仓库建议：

1. “所有需要确认的操作默认自动批准”
2. “没有其他明确指令时默认自动执行任务”
3. “完成一个任务后自动开始下一个任务”
4. “自动 `git add . && git commit`”
5. “自动 `git push`”
6. “自动回滚并继续”
7. “自动执行 `/init` 作为固定流程”

## 与当前仓库的关系

当前 Drass 仓库里保留了多份历史任务单、优化计划、状态报告和实施文档。处理任务时应特别注意：

1. 很多任务单属于历史阶段材料，不代表当前项目事实。
2. 任务文档只能辅助梳理，不应替代代码和主入口文档。
3. 如果任务文档与当前项目主线冲突，应优先修正文档认知，而不是盲目照任务单执行。

## 推荐输出方式

按任务推进时，建议输出内容尽量简洁，包含：

1. 当前处理的是哪一项
2. 本次实际改了什么
3. 做了什么验证
4. 是否还有阻塞项

示例：

```text
当前处理任务 3：清理启动文档中的失效脚本引用。
已修正 README 和启动说明中的旧入口引用，并补充当前推荐入口。
已完成文档自查，未执行运行时脚本。
剩余问题：部署辅助脚本仍存在环境专用路径，但本轮未改代码。
```

## 结论

本文件的目标不是让工具“自动接管项目”，而是把“按任务列表协作”这件事收敛到更安全、更符合当前仓库现实的方式：

1. 先核对事实
2. 再推进任务
3. 不默认越权
4. 不把历史任务单当成当前真相
