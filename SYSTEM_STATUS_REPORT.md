# 系统状态报告

> 状态：历史环境快照，不作为当前项目统一运行状态说明。

## 文档定位

这份文件原本描述的是某一台 Ubuntu AMD GPU 机器在某个时间点的运行现场。它更像一次部署现场快照，而不是仓库级的长期有效文档。

## 为什么需要降级

旧版内容直接写入了大量只对单次现场成立的信息，例如：

1. 固定 PID
2. 固定模型名称和 GPU 占用率
3. 固定日志路径
4. 固定宿主机目录
5. 固定服务健康状态

这些内容一旦脱离当时环境，就会迅速失效，并误导后来读者把它当成项目标准运行状态。

## 当前更准确的理解方式

应把该文件理解为：

1. 一次历史部署现场记录
2. 一条 Ubuntu 服务化部署路径的运行样例
3. 对 `deployment/scripts/start-ubuntu-services.sh` 周边环境的补充背景

## 当前应参考什么

如果你需要当前可执行信息，请改看：

1. `README.md`
2. `docs/ONE_CLICK_STARTUP_GUIDE.md`
3. `deployment/docs/ubuntu-amd-deployment.md`
4. `docs/chensha_部署与基础设施规则.md`
5. 实际脚本、配置文件和当前机器实时状态

## 本文件保留用途

保留该文件，仅作为历史状态快照；后续不要再把其中端口、模型、路径、日志或健康状态结论直接复制到主文档中。
